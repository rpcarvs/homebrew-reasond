# homebrew-reasond

Homebrew tap for installing `reasond`.

`reasond` is a lightweight local tool to audit coding agent reasoning.

## Install

```bash
brew tap rpcarvs/reasond
brew install reasond
```

## Upgrade

```bash
brew update
brew upgrade reasond
```

## More information

Project repository:

- https://github.com/rpcarvs/reasond
